ENVIRONMENT=development
FRONTEND_ORIGIN=http://localhost:3000
LOG_LEVEL=INFO
# full = local dev with torch/RAG/embeddings. lite = Render 512Mi (no ML on this host).
BACKEND_MODE=full
# Get a key at https://aistudio.google.com/
GEMINI_API_KEY=
# Only needed for full mode or the separate HF Emotion Space:
HF_TOKEN=
HF_MODEL_REPO=DSAditya552003/emosense-ai-model
DEFER_ML_LOAD=true
LOAD_EMOTION_MODEL=true
